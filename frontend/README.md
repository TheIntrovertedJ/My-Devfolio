# Frontend

This folder holds the frontend source for the Devfolio project.

Structure:

- `src/components` — UI components
- `src/pages` — page-level components
- `src/styles` — CSS / styling files
- `public/assets` — public static assets
